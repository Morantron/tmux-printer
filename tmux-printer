#!/usr/bin/env bash

# TODO proper regexp with capturing group
#PATTERN_FORMAT="#\[\([^]]*\)\]"
PATTERN_FORMAT="#\[\([^]]*\)\]"

# TODO use tput

if [[ $1 == '--debug' ]]; then
  cat | awk -f ./tmux-printer.awk
else
  eval "echo -e $(cat | awk -f ./tmux-printer.awk)"
fi
